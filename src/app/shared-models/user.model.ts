import {EmailValidator} from '@angular/forms';

export class User {
  email: string;
  firstName: string;
  lastName: string;
  role: string;
  password: string;
  securityQuestion: string;
  securityAnswer: string;
}
